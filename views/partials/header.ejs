<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body>
    <header class="header">
        <h1>JTE Collective</h1>
        <div class="nav-buttons">
            <a href="/listings"><button class="button">Browse</button></a>
            <% if (user) { %>
                <a href="/listings/new"><button class="button">New Listing</button></a>
                <a href="/logout"><button class="button">Log Out</button></a>
            <% } else { %>
                <a href="/auth/google" class="button"><button class="button">Login</button></a>
            <% } %>    </div>
      </header>
